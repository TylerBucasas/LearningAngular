import { FindContentWithTitlePipe } from './find-content-with-title.pipe';

describe('FindContentWithTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FindContentWithTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
